<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Only the splash window needs a colour resource — everything else is themed
      in Compose from ui/theme/Color.kt. Keep this in sync with
      backgroundGradientLightStart, and the values-night copy with
      backgroundGradientDarkEnd, so the splash hands over without a flash.
    -->
    <color name="splash_background">#FFF5E6</color>
</resources>
